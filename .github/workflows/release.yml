name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: shivammathur/setup-php@v2
        with:
          php-version: '8.2'
          tools: composer:v2

      - name: Install dependencies
        run: composer install --no-interaction --no-progress --prefer-dist

      - name: Run strict technical gate
        run: composer release:check

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          # Allow attaching to a release that was already created (e.g. via
          # the API). Fails cleanly if the tag does not exist.
          fail_on_unmatched_files: false
          generate_release_notes: true
          body: |
            ## Release ${{ github.ref_name }}

            ### Changes
            Please see the [CHANGELOG](CHANGELOG.md) for details.

            ### Installation
            ```bash
            composer create-project sirosoft/api my-api
            ```
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
