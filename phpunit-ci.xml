<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="https://schema.phpunit.de/11.5/phpunit.xsd"
         bootstrap="tests/bootstrap.php"
         colors="true"
         cacheDirectory=".phpunit.cache"
         executionOrder="default"
         beStrictAboutOutputDuringTests="false"
         failOnRisky="false"
         failOnWarning="false"
>
    <php>
        <ini name="memory_limit" value="512M"/>
    </php>

    <testsuites>
        <testsuite name="Unit">
            <directory suffix="Test.php">tests/unit</directory>
        </testsuite>
        <testsuite name="Integration">
            <directory suffix="Test.php">tests/integration</directory>
        </testsuite>
        <testsuite name="Fuzz">
            <directory suffix="Test.php">tests/fuzz</directory>
        </testsuite>
        <testsuite name="DAST">
            <directory suffix="Test.php">tests/dast</directory>
        </testsuite>
    </testsuites>

    <!-- Coverage filter: only present so coverage jobs, which pass
         coverage flags on the CLI, collect data. Harmless for runs without
         a coverage driver. -->
    <source>
        <include>
            <directory suffix=".php">.</directory>
        </include>
        <exclude>
            <directory>tests</directory>
            <directory>vendor</directory>
            <directory>scripts</directory>
            <directory>coverage</directory>
        </exclude>
    </source>

</phpunit>
